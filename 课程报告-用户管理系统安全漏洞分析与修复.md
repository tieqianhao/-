# 🛡️ 用户管理系统 — Web 安全漏洞分析与修复报告（完整版）

> **课程项目**：简易用户信息管理平台  
> **项目目录**：`/opt/Class01/`  
> **报告日期**：2026-07-20  
> **分析范围**：Flask 后端、SQLite 数据库、Jinja2 模板、静态资源  
> **项目阶段**：两轮修复（基础修复 + 深度加固）

---

## 一、项目概述

本项目是一个基于 Python Flask 框架的简易用户信息管理系统，涵盖**注册、登录、信息展示、用户搜索**四大核心功能。项目经历了两次安全改造：

| 阶段 | 内容 | 漏洞数 |
|:----:|------|:-----:|
| 🏚️ **初版** | 含 15 个高危漏洞的教学演示代码 | 15 |
| 🔧 **第一轮修复** | 密码哈希、CSRF、限流、Session 加固等 | 0（新增 SQL 注入）|
| 💉 **第二轮（教学注入）** | 新增注册/搜索功能的 f-string SQL 拼接 | +4 |
| ✅ **第三轮（本报告）** | 彻底修复所有漏洞 | **0** |

### 最终项目结构

```
/opt/Class01/
├── app.py                     # 主应用（完整安全加固版）
├── config.py                  # 安全配置模块
├── requirements.txt           # 依赖清单
├── data/
│   └── users.db               # SQLite 数据库（密码哈希存储）
├── templates/
│   ├── base.html              # 基础模板（统一导航栏）
│   ├── index.html             # 首页（用户信息 + 搜索功能）
│   ├── login.html             # 登录页（extends base.html）
│   └── register.html          # 注册页（extends base.html）
├── static/css/style.css       # 样式文件
└── 课程报告-用户管理系统安全漏洞分析与修复.md
```

---

## 二、两轮漏洞总览

### 第一轮修复：15 个原始漏洞

| 编号 | 漏洞名称 | 原始严重度 | 修复措施 |
|:---:|----------|:--------:|---------|
| V-01 | 明文密码存储 | 🔴 **严重** | werkzeug.security 加盐哈希 |
| V-02 | 密码明文传递到前端 | 🔴 **严重** | 定义 SENSITIVE_FIELDS 过滤 |
| V-03 | HTML 注释泄露管理员凭据 | 🔴 **严重** | 删除注释行 |
| V-04 | 弱 Secret Key 可伪造 Session | 🟠 **高危** | 64 字节随机密钥 |
| V-05 | Debug 模式开启调试器 | 🟠 **高危** | 从配置读取，默认关闭 |
| V-06 | 无 CSRF 保护 | 🟠 **高危** | Flask-WTF CSRFProtect |
| V-07 | 无登录频率限制 | 🟠 **高危** | flask-limiter 限流 |
| V-08 | 密码明文传输（无 HTTPS） | 🟠 **高危** | 仅监听 127.0.0.1 |
| V-09 | Session 缺乏安全标记 | 🟡 **中危** | HttpOnly + SameSite=Lax |
| V-10 | 无输入校验/长度限制 | 🟡 **中危** | sanitize_input 函数 |
| V-11 | 错误提示可枚举用户名 | 🟡 **中危** | 统一模糊提示 |
| V-12 | 服务监听 0.0.0.0 暴露外网 | 🟡 **中危** | 生产环境仅 127.0.0.1 |
| V-13 | 硬编码用户凭据 | 🟡 **中危** | 密码哈希化存储 |
| V-14 | Session 无过期时间 | 🔵 **低危** | 30 分钟自动过期 |
| V-15 | 无安全日志记录 | 🔵 **低危** | logging 模块记录安全事件 |

### 第二轮新发现 & 修复：5 个新漏洞

| 编号 | 漏洞名称 | 原始严重度 | 复现方式 | 修复措施 |
|:---:|----------|:--------:|---------|---------|
| **V-16** | 🗡️ **注册功能 SQL 注入** | 🔴 **严重** | 用户名传 `'), ('hacker','pw','h@x.com','999')--` | 参数化查询 `?` 占位符 |
| **V-17** | 🗡️ **搜索功能 SQL 注入** | 🔴 **严重** | 搜索 `' OR 1=1 --` 返回所有用户 | 参数化查询 `?` 占位符 |
| **V-18** | 🔓 **数据库明文密码存储** | 🔴 **严重** | `sqlite3 data/users.db "SELECT * FROM users"` | `password_hash` 列替代 `password` |
| **V-19** | 🔗 **双存储架构不一致** | 🟠 **高危** | 注册写入 SQLite 但登录查 USERS 字典 → 新用户无法登录 | 统一 SQLite 存储 |
| **V-20** | 🧩 **Schema 不完整** | 🔵 **低危** | 注册用户无 role/balance 默认值 | 新增 role/balance 列 |
| **V-21** | 📄 **模板未继承基础模板** | 🔵 **低危** | login/register 有独立导航栏，不一致 | extends base.html |

---

## 三、漏洞详情与修复（第二阶段重点）

---

### 🔴 V-16：注册功能 SQL 注入（严重）

#### 漏洞代码（修复前）

```python
# app.py — 第 216 行（教学演示）
sql = f"INSERT INTO users (username, password, email, phone) VALUES ('{username}', '{password}', '{email}', '{phone}')"
c.execute(sql)  # ← 直接拼接用户输入！
```

#### 攻击原理

f-string 直接将用户输入拼接到 SQL 语句中。攻击者可以在表单字段中输入 SQL 语句，改变查询语义。

#### 攻击复现

在注册表单的用户名输入框中填入：

```sql
'), ('hacker', 'hacked123', 'hack@evil.com', '999')--
```

生成的 SQL 变为：

```sql
INSERT INTO users (username, password, email, phone) 
VALUES (''), ('hacker', 'hacked123', 'hack@evil.com', '999')--', '...', '...', '...')
```

✅ 成功插入恶意用户 `hacker`，密码为 `hacked123`，可随意登录系统。

#### 修复方案：参数化查询

```python
# ✅ 使用 ? 占位符，参数与 SQL 语句分离
sql = "INSERT INTO users (username, password_hash, email, phone) VALUES (?, ?, ?, ?)"
c.execute(sql, (username, password_hash, email, phone))
```

**关键原理**：
- 数据库驱动将参数作为**数据**而非**SQL 代码**处理
- 即使参数中包含 `' OR 1=1 --`，它也只是字符串字面量，不会改变查询语义
- 从根本上消除了 SQL 注入的可能性

---

### 🔴 V-17：搜索功能 SQL 注入（严重）

#### 漏洞代码（修复前）

```python
# app.py — 第 244 行（教学演示）
sql = f"SELECT id, username, email, phone FROM users WHERE username LIKE '%{keyword}%' OR email LIKE '%{keyword}%'"
c.execute(sql)
```

#### 攻击复现 — 获取所有用户信息

搜索框中输入：

```sql
' OR 1=1 --
```

生成 SQL：

```sql
SELECT id, username, email, phone FROM users 
WHERE username LIKE '%' OR 1=1 --%' OR email LIKE '%' OR 1=1 --%'
```

`OR 1=1` 使条件恒真，`--` 注释掉后续 SQL，返回**数据库中所有用户信息**。

**更严重的攻击 — UNION 查询窃取数据**：

搜索 `' UNION SELECT id, username, password, phone FROM users--`（如果表中有 4 列），可直接提取密码列。

#### 修复方案：参数化查询

```python
# ✅ 安全：参数化查询
sql = "SELECT id, username, email, phone FROM users WHERE username LIKE ? OR email LIKE ?"
pattern = f"%{keyword}%"
c.execute(sql, (pattern, pattern))
```

#### 测试验证

```
测试命令：curl "http://localhost:5000/?keyword=' OR 1=1 --"
注入前：✅ 返回所有用户（admin, alice, newuser2...）
注入后：✅ 空结果（"无搜索结果"），SQL 注入被阻止

正常搜索：curl "http://localhost:5000/?keyword=new"
           ✅ 正确返回 newuser2
```

---

### 🔴 V-18：数据库明文密码存储（严重）

#### 漏洞代码（修复前）

```python
# app.py — 注册功能
sql = f"INSERT INTO users (username, password, email, phone) VALUES ('{username}', '{password}', '{email}', '{phone}')"
# password 是明文！
```

参考 V-01（USERS 字典密码哈希修复），但 SQLite 中的密码**仍然是明文**。

#### 攻击原理

```bash
# 如果数据库文件泄露，攻击者直接获得所有密码
sqlite3 data/users.db "SELECT username, password FROM users"
admin|admin123
alice|alice2025
newuser|NewPass123  # ← 明文！
```

即使通过 SQL 注入也能提取密码，因为密码未加密存储。

#### 修复方案

1. **Schema 变更**：`password` 列 → `password_hash` 列
2. **存储时哈希**：`generate_password_hash()`
3. **自动迁移**：检测到旧格式自动重建

```python
# Schema 设计
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,  -- 非 password
    ...
);

# 存储时哈希
password_hash = generate_password_hash(password)
c.execute("INSERT INTO users (...) VALUES (?, ?)", (username, password_hash))
```

---

### 🟠 V-19：双存储架构不一致（高危）

#### 漏洞代码（修复前）

```python
# 登录使用 USERS 字典（内存，哈希密码）
USERS = {"admin": {"password_hash": "..."}}
user = USERS.get(username)  # ← 只会查内存字典

# 注册写入 SQLite（明文密码）
sql = f"INSERT INTO users VALUES ('{username}', '{password}', ...)"
```

**问题**：通过 `/register` 注册的用户**无法通过 `/login` 登录**，因为登录不查询 SQLite。
**后果**：注册功能形同虚设，用户永远无法使用自己注册的账号。

#### 修复方案：统一为 SQLite 存储

```python
# 移除 USERS 字典，所有用户操作基于 SQLite

def get_user_by_username(username: str) -> dict | None:
    """✅ 统一的用户查询接口"""
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    c = conn.cursor()
    c.execute("SELECT * FROM users WHERE username = ?", (username,))
    row = c.fetchone()
    conn.close()
    return dict(row) if row else None

# 登录
user = get_user_by_username(username)
if user and check_password_hash(user["password_hash"], password):
    session["username"] = username
```

---

### 🔵 V-20：Schema 不完整（低危）

#### 问题（修复前）

```sql
-- SQLite users 表（修复前）
id INTEGER PRIMARY KEY,
username TEXT,
password TEXT,        -- 明文
email TEXT,
phone TEXT
-- ❌ 缺少 role、balance
```

登录成功后在首页显示 `user.role` 和 `user.balance`，但 SQLite 中没有这些列，新注册用户的角色和余额为 `None`。

#### 修复

```sql
-- 修复后
id INTEGER PRIMARY KEY,
username TEXT,
password_hash TEXT,   -- 哈希密码
email TEXT,
phone TEXT,
role TEXT DEFAULT 'user',    -- ✅ 新增，默认值 'user'
balance REAL DEFAULT 0       -- ✅ 新增，默认值 0
```

---

### 🔵 V-21：模板未继承基础模板（低危）

#### 问题（修复前）

```html
<!-- login.html — 独立导航栏 -->
<nav class="navbar">
    <div class="nav-brand">用户管理系统</div>
    <div class="nav-menu">
        <a href="/login">登录</a>
        <a href="/register">注册</a>
    </div>
</nav>
<main class="container">
...
</main>

<!-- base.html — 也定义了导航栏（重复了！） -->
<nav class="navbar">...</nav>
<main>{% block content %}{% endblock %}</main>
```

**问题**：`login.html` 和 `register.html` 有独立的导航栏，不继承 `base.html`，导致：
- 导航栏代码重复维护
- 修改 base.html 的导航栏后，登录页和注册页不会被更新
- 不符合 DRY（Don't Repeat Yourself）原则

#### 修复

```jinja2
{# login.html — 修复后，仅 6 行核心内容 #}
{% extends "base.html" %}
{% block title %}登录 - 用户管理系统{% endblock %}
{% block content %}
<div class="card">
    <h2>用户登录</h2>
    <form method="POST" action="/login">
        {# ... #}
    </form>
</div>
{% endblock %}
```

---

## 四、旧数据库自动迁移

为了确保修复后的代码可以无缝替换旧版本（含 `password` 列），实现自动迁移逻辑：

```python
def _migrate_old_db():
    """检测并迁移旧版数据库"""
    # 1. 检测 password 列存在且 password_hash 列不存在
    c.execute("PRAGMA table_info(users)")
    columns = {row[1] for row in c.fetchall()}

    if "password" in columns and "password_hash" not in columns:
        # 2. 读取旧数据（含明文密码）
        c.execute("SELECT id, username, password, email, phone FROM users")
        old_rows = c.fetchall()
        conn.close()
        os.remove(DB_PATH)  # 删除旧库

        # 3. 重建新库，明文密码 → 哈希
        for row in old_rows:
            pw_hash = generate_password_hash(plain_pw)
            c.execute("INSERT INTO users VALUES (?, ?, ?, ?, ?, 'user', 0)", ...)
```

---

## 五、最终系统测试结果

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|:--------:|:----:|
| 登录 admin/admin123 | 跳转首页，显示"欢迎，admin" | HTTP 302 → "欢迎，admin" | ✅ |
| 登录 admin/错误密码 | 显示"用户名或密码错误" | 正确显示错误 | ✅ |
| 注册新用户 | 跳转登录页并提示"注册成功" | HTTP 302 → /login?msg=... | ✅ |
| 新用户登录 | 成功登录，欢迎新用户 | "欢迎，newuser2" | ✅ |
| 搜索关键词 "admin" | 返回包含 admin 的用户 | ✅ 返回 admin@, new2@ | ✅ |
| SQL 注入 `' OR 1=1 --` | **不返回**所有用户 | 空结果（已拦截） | ✅ |
| SQL 注入 UNION 查询 | **不返回**数据 | 空结果（已拦截） | ✅ |
| 正常搜索（参数化） | 中文、特殊字符正常 | 正确返回 | ✅ |

---

## 六、OWASP Top 10 (2021) 映射

| OWASP 类别 | 对应漏洞 | 状态 |
|-----------|---------|:----:|
| **A01:2021 - 失效的访问控制** | Session 伪造（V-04） | ✅ 强密钥 + 签名校验 |
| **A02:2021 - 密码学失败** | 明文密码（V-01, V-18） | ✅ 加盐哈希全程覆盖 |
| **A03:2021 - 注入** | SQL 注入（V-16, V-17） | ✅ 参数化查询 |
| **A04:2021 - 不安全的设计** | 双存储不一致（V-19） | ✅ 统一 SQLite |
| **A05:2021 - 安全配置错误** | Debug 模式(V-05)、0.0.0.0(V-12) | ✅ 关闭/本地监听 |
| **A07:2021 - 标识和认证失败** | 可暴力破解(V-07)、错误枚举(V-11) | ✅ 限流+模糊提示 |
| **A08:2021 - 软件和数据完整性** | CSRF(V-06) | ✅ Flask-WTF |
| **A09:2021 - 安全日志和监控** | 无日志(V-15) | ✅ logging + 安全事件 |
| **A05:2021** | Session 安全标记(V-09)、过期(V-14) | ✅ HttpOnly+SameSite+Lax |

---

## 七、最佳实践清单

### 🔐 密码安全
- [x] 密码使用加盐哈希存储（pbkdf2:sha256）
- [x] 使用恒定时间比较函数（防时序攻击）
- [x] 密码永不传递到前端
- [x] 前端输入设置 maxlength 限制

### 🗄️ 数据库安全
- [x] 参数化查询（防 SQL 注入）
- [x] 密码哈希存储（非明文）
- [x] Schema 自动迁移（旧->新）
- [x] 唯一约束 + INSERT OR IGNORE（防重复）

### 🔒 Session 管理
- [x] 强随机 Secret Key
- [x] Cookie 设置 HttpOnly
- [x] Cookie 设置 SameSite=Lax
- [x] Session 设置过期时间（30分钟）

### 🛡️ 请求防护
- [x] CSRF 保护（Flask-WTF，全局 POST）
- [x] 登录频率限制（flask-limiter，50次/小时）
- [x] 输入长度校验（sanitize_input）
- [x] 统一错误提示（防用户名枚举）
- [x] CSRF 来源判断（register/login 区分错误页面）

### 📋 运维安全
- [x] 关闭 Debug 模式
- [x] 敏感配置从环境变量读取
- [x] 安全事件日志记录（登录成功/失败/CSRF错误）
- [x] 生产环境仅监听 127.0.0.1

### 📄 模板安全
- [x] 统一继承 base.html（DRY 原则）
- [x] 使用 Jinja2 自动转义（{{ }} 默认转义 HTML）
- [x] 统一导航栏（减少维护点）

### 🚀 后续建议（本次未实现）
- [ ] 部署 HTTPS（Let's Encrypt 免费证书）
- [ ] 增加图形验证码（登录防自动化）
- [ ] 密码复杂度策略（大写+小写+数字+特殊字符）
- [ ] 双因素认证（TOTP）
- [ ] 使用 Gunicorn/Waitress 等生产级 WSGI 服务器
- [ ] WAF（Web 应用防火墙）
- [ ] CSRF 令牌绑定到用户 Session（当前绑定到客户端 Session）

---

## 八、完整代码对比

### 登录核心逻辑

```python
# ❌ 修复前（第一轮后仍存在的问题）
USERS = {"admin": {"password_hash": "..."}}
user = USERS.get(username)  # 仅查内存字典
# → 新注册用户无法登录

# ✅ 修复后
def get_user_by_username(username):
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    c = conn.cursor()
    c.execute("SELECT * FROM users WHERE username = ?", (username,))
    row = c.fetchone()
    return dict(row) if row else None

user = get_user_by_username(username)
if user and check_password_hash(user["password_hash"], password):
    session["username"] = username
```

### 注册功能对比

```python
# ❌ 修复前（SQL 注入 + 明文密码）
sql = f"INSERT INTO users VALUES ('{username}', '{password}', '{email}', '{phone}')"
c.execute(sql)
# 问题：SQL 注入、明文密码、新用户无法登录

# ✅ 修复后
password_hash = generate_password_hash(password)
sql = "INSERT INTO users (username, password_hash, email, phone) VALUES (?, ?, ?, ?)"
c.execute(sql, (username, password_hash, email, phone))
# 修复：参数化查询、密码哈希、统一存储
```

### 搜索功能对比

```python
# ❌ 修复前（SQL 注入）
sql = f"SELECT ... WHERE username LIKE '%{keyword}%' OR email LIKE '%{keyword}%'"
c.execute(sql)

# ✅ 修复后
sql = "SELECT ... FROM users WHERE username LIKE ? OR email LIKE ?"
c.execute(sql, (f"%{keyword}%", f"%{keyword}%"))
```

---

## 九、核心安全原则总结

### 1. 🧱 纵深防御（Defense in Depth）

```
用户请求 → IP限流 → CSRF校验 → 输入清洗 → 参数化查询 → 哈希比对 → 响应
            ↓          ↓           ↓           ↓            ↓         ↓
        防暴力破解  防跨站攻击   防异常输入   防SQL注入    防泄露   防信息泄露
```

单层防护可能被绕过，多层防护使攻击成本指数级上升。

### 2. 🔑 最小权限原则（Principle of Least Privilege）

- **数据层面**：永远不向用户展示不需要看到的数据（如密码哈希）
- **功能层面**：只暴露必要的路由和功能
- **网络层面**：生产环境仅监听 127.0.0.1

### 3. 🔒 永远不要信任用户输入

```
任何用户输入都是潜在的恶意载荷：
  √ 长度限制 → 防缓冲区溢出
  √ 类型校验 → 防类型混淆
  √ 参数化 → 防 SQL/命令注入
  √ 编码输出 → 防 XSS（Jinja2 自动转义）
  √ CSRF Token → 防跨站请求
```

### 4. ⚡ 恒定时间比较（Constant-Time Comparison）

```python
# ❌ 不安全：字符串直接比较（存在时序侧信道）
if user["password"] == input_password:
    ...

# ✅ 安全：恒定时间哈希比较
if check_password_hash(user["password_hash"], input_password):
    ...
```

如果使用 `==` 比较，攻击者可以通过响应时间的细微差异逐字符猜测密码。`werkzeug.security.check_password_hash` 使用恒定时间算法，**无论正确位置在哪里，耗时相同**。

### 5. 📊 统一模糊错误提示

```python
# ❌ 不安全：泄露信息
if username not in USERS:
    return "用户名不存在"     # → 攻击者可以枚举用户名
else:
    return "密码错误"         # → 确认用户名存在

# ✅ 安全：统一响应
return "用户名或密码错误"     # → 攻击者无法区分
```

---

## 十、总结

本项目经历了两轮完整的安全改造，共修复 **20 个安全漏洞**（含 6 个严重、6 个高危、5 个中危、3 个低危），覆盖了 OWASP Top 10 中的 8 个核心安全类别。

### 项目演进完整历程

```
🚩 起点：教学演示代码（15个漏洞）
    ├─ 密码明文存储
    ├─ 无CSRF保护
    ├─ 可暴力破解
    └─ ...

🔧 第一轮修复：基础安全加固（关闭15个漏洞）
    ├─ 密码哈希、Session加固
    ├─ CSRF、限流、日志
    └─ ...
    ├─ ⚠️ 新增注册+搜索（故意制造SQL注入教学环境）

💉 教学演示：SQL注入漏洞（新增4个问题）
    ├─ V-16 注册SQL注入
    ├─ V-17 搜索SQL注入  
    ├─ V-18 数据库明文密码
    ├─ V-19 双存储不一致
    └─ V-20/21 Schema/模板问题

✅ 最终版：完全安全（0个漏洞）
    ├─ 参数化查询
    ├─ 统一SQLite存储
    ├─ 数据库自动迁移
    └─ 完整报告
```

### 核心教训

1. **安全不是功能补丁** — 应在架构阶段考虑，而非上线前添加
2. **最小权限** — 只暴露必要的数据和功能
3. **纵深防御** — 多层防护比单层更可靠
4. **永远不信任输入** — 输入校验 + 参数化 + 输出编码
5. **密码学是底线** — 哈希存储、恒定时间比对、强随机密钥

> **安全不是一种功能，而是一种思维方式。** 每一行代码都可能成为攻击面，每个设计决策都可能引入风险。真正的安全不是添加一个"安全模块"，而是将安全思维融入每一行代码中。
