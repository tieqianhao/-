# 🛡️ 个人中心与充值功能 — 越权与绕过漏洞修复报告

> **项目名称**：用户信息管理系统  
> **报告日期**：2026-07-22  
> **修复范围**：`/profile` 个人中心、`/recharge` 充值模块  
> **漏洞来源**：开发需求强制不安全设计，后端无权限校验、无参数校验

---

## 一、漏洞概述

本次测试针对 Flask 用户系统新增的**个人中心（/profile）**、**余额充值（/recharge）** 模块进行安全审计。因业务逻辑设计存在严重安全缺陷，共发现 **4 个高危/严重漏洞**：

| 编号 | 漏洞名称 | 风险等级 | 漏洞核心影响 |
|:----:|---------|:--------:|-------------|
| **V-27** | 全量用户信息越权查询 | 🟠 **高危** | 任意用户可查看全站所有用户隐私信息 |
| **V-28** | 任意用户非法扣款（负数充值） | 🔴 **严重** | 攻击者可扣除/清空任意用户余额 |
| **V-29** | 任意用户无限刷余额 | 🔴 **严重** | 攻击者可随意为任意账号充值无限余额 |
| **V-30** | 身份绑定失效（核心根源） | 🔴 **严重** | 服务端完全信任前端参数，权限体系形同虚设 |

---

## 二、漏洞详情与修复方案

---

### 🔴 V-27：全量用户信息越权查询（IDOR）

#### 漏洞代码（修复前）

```python
@app.route("/profile", methods=["GET"])
def profile():
    # ❌ 用户身份完全由前端URL参数控制
    user_id = request.args.get("user_id", type=int)
    if user_id is None:
        return render_template("profile.html", error="缺少 user_id 参数")

    user = get_safe_user_by_id(user_id)
    if user is None:
        return render_template("profile.html", error="用户不存在")

    return render_template("profile.html", user=user)
```

#### 攻击原理

服务端信任前端 URL 参数 `user_id`，未校验当前登录会话与查询用户的归属关系。攻击者只需修改 URL 中的 `user_id` 参数值，即可遍历系统内所有用户资料。

#### 复现步骤

```bash
# 普通用户登录后，修改URL参数
curl http://target.com/profile?user_id=1    # 查看admin资料
curl http://target.com/profile?user_id=2    # 查看alice资料
curl http://target.com/profile?user_id=3    # 查看任意用户资料
# ✅ 页面返回目标用户的ID、用户名、邮箱、手机号、余额
```

#### 修复方案

从 **session 会话**读取当前登录用户身份，彻底拒绝前端 `user_id` 参数：

```python
@app.route("/profile", methods=["GET"])
def profile():
    # ✅ 从 session 读取登录用户
    username = session.get("username")
    if not username:
        return redirect("/login")

    user = get_safe_user_info(username)
    if user is None:
        return render_template("profile.html", error="用户不存在")

    return render_template("profile.html", user=user)
```

---

### 🔴 V-28：任意用户非法扣款（负数充值）

#### 漏洞代码（修复前）

```python
@app.route("/recharge", methods=["POST"])
def recharge():
    user_id = request.form.get("user_id", type=int)
    amount = request.form.get("amount", type=float, default=0)

    # ❌ 未校验 amount 正负！传入负数等价于扣款
    c.execute("UPDATE users SET balance = balance + ? WHERE id = ?", (amount, user_id))
```

#### 攻击原理

后端直接执行 `balance = balance + amount`，当 `amount` 为负数时，等价于对目标用户进行扣款操作，且无任何拦截。

#### 复现步骤

```bash
# 登录后构造负数金额请求
curl -X POST http://target.com/recharge \
  -d "user_id=1&amount=-99999"
# ✅ 管理员余额被扣除99999
```

#### 修复方案

```python
# ✅ 金额正数校验
amount = request.form.get("amount", type=float, default=0)
if amount <= 0:
    logger.warning("Recharge rejected: invalid amount %f", amount)
    return render_template("profile.html", error="充值金额必须大于 0")
```

---

### 🔴 V-29：任意用户无限刷余额（越权充值）

#### 漏洞代码（修复前）

```python
@app.route("/recharge", methods=["POST"])
def recharge():
    # ❌ 充值目标由前端表单参数控制
    user_id = request.form.get("user_id", type=int)

    # ❌ 无任何金额上限限制
    c.execute("UPDATE users SET balance = balance + ? WHERE id = ?", (amount, user_id))
```

#### 攻击原理

`user_id` 来自前端表单，攻击者可指定任意账号为充值目标，重复提交实现无限刷余额，或为他人恶意充值扰乱系统资产平衡。

#### 修复方案

```python
@app.route("/recharge", methods=["POST"])
def recharge():
    # ✅ 从 session 获取当前登录用户
    username = session.get("username")
    if not username:
        return redirect("/login")

    user = get_user_by_username(username)

    # ✅ 仅操作当前登录用户的余额
    c.execute("UPDATE users SET balance = balance + ? WHERE id = ?",
              (amount, user["id"]))
```

---

### 🔴 V-30：身份绑定失效（核心根源漏洞）

#### 漏洞代码（修复前）

```python
# ❌ profile: 从前端URL获取user_id
user_id = request.args.get("user_id", type=int)

# ❌ recharge: 从前端表单获取user_id
user_id = request.form.get("user_id", type=int)
```

#### 攻击原理

正常安全设计中，用户操作身份必须从**服务端 session 会话**读取，保证操作人与登录人一致。本系统所有新增功能均强制从前端 URL/表单获取 `user_id`，服务端完全信任前端可控参数，**彻底丧失身份校验能力**。

#### 危害

- **所有权限控制体系形同虚设**
- 任何前端可控参数均可篡改操作目标
- 是越权查询（V-27）、恶意充值（V-28）、刷余额（V-29）**所有漏洞的核心根源**

#### 修复方案

**全局原则：涉及用户信息/资产的操作，统一从 session 读取身份：**

```python
# ✅ 所有接口统一从 session 获取用户身份
username = session.get("username")
user = get_user_by_username(username)

# 后续操作基于 user["id"]，而非前端参数
c.execute("UPDATE users SET balance = balance + ? WHERE id = ?",
          (amount, user["id"]))
```

---

## 三、修复前后对比

### 个人中心

| 维度 | 修复前 | 修复后 |
|:----|:------|:-------|
| 身份来源 | 前端 URL 参数 `user_id` | 服务端 `session["username"]` |
| 是否需要登录 | ❌ 否 | ✅ 必须登录 |
| URL 参数是否生效 | ✅ `?user_id=2` 可看 alice | ❌ 忽略所有 URL 参数 |
| 可查看范围 | 全站任意用户 | 仅当前登录用户本人 |

### 充值功能

| 维度 | 修复前 | 修复后 |
|:----|:------|:-------|
| 操作目标来源 | 前端表单 `user_id` | 服务端 `session` |
| 金额正负校验 | ❌ 无 | ✅ `amount > 0` |
| 可操作范围 | 全站任意用户 | 仅当前登录用户本人 |
| 审计日志 | ❌ 无详细日志 | ✅ 记录用户、金额、余额变化、IP |

---

## 四、最终安全测试结果

| 测试用例 | 预期结果 | 实际结果 | 状态 |
|---------|---------|:--------:|:----:|
| 登录后访问 profile | 显示当前用户信息 | admin@example, 13800138000 | ✅ |
| 未登录访问 profile | 重定向到 /login | 302 → /login | ✅ |
| URL 传 user_id=2 | 忽略参数，仍显示当前用户 | 显示 admin 资料 | ✅ |
| 正常充值 +100 | 余额增加，跳转 profile | HTTP 302, 余额正确 | ✅ |
| 负数充值 -500 | 拦截，显示错误提示 | "充值金额必须大于 0" | ✅ |
| 零充值 0 | 拦截，显示错误提示 | "充值金额必须大于 0" | ✅ |
| 未登录发起充值 | CSRF 拦截 / 重定向 | 400 (CSRF保护) | ✅ |
| 导航栏"个人中心" | 链接到 /profile | 正确 | ✅ |

---

## 五、安全加固总结

### 修复的漏洞

| 漏洞 | 严重度 | 修复方式 | 核心措施 |
|:----|:------:|---------|---------|
| V-27 IDOR 越权查询 | 🟠 高危 | session 身份绑定 | 禁止前端传 user_id |
| V-28 负数充值 | 🔴 严重 | 金额正数校验 | amount > 0 拦截 |
| V-29 越权充值 | 🔴 严重 | session 身份锁定 | 仅操作当前用户 |
| V-30 身份绑定失效 | 🔴 严重 | 全局架构修复 | 所有接口从 session 读身份 |

### 已实现的安全措施

- [x] **身份强制绑定**：profile/recharge 从 session 读取用户身份
- [x] **金额正数校验**：拦截负数/零充值请求
- [x] **操作人锁定**：仅操作当前登录用户余额
- [x] **CSRF 保护**：Flask-WTF 全局防护
- [x] **登录频率限制**：flask-limiter 50次/小时
- [x] **登录校验**：未登录重定向到 /login
- [x] **审计日志**：记录用户名、金额、余额变化、客户端 IP
- [x] **配置安全分离**：敏感配置从 config.py + 环境变量读取
